# Mission-specific content (local only, not shared)
missions/

# Local Retribution working copies (the canonical repo is github.com/bradyccox/414Ret).
# Kept out of this repo so full clones aren't accidentally committed as gitlinks.
414Ret/
retribution-*/

# Large binaries
*.miz
*.zip
*.pdf

# Extracted mission internals and scratch workspace
tmp/
miz_extracted/
*_extracted/

# MOOSE framework (download separately)
Moose.lua

# Editor backups
*.bak
*.tmp
*~

# OS junk
Thumbs.db
.DS_Store
